services:
  rt-refresh:
    build: .
    image: rt-refresh:local
    container_name: rt-refresh
    restart: unless-stopped
    environment:
      HOST: 0.0.0.0
      PORT: 8787
      AUTH_USER: ${AUTH_USER:-admin}
      AUTH_PASSWORD: ${AUTH_PASSWORD:-}
    ports:
      - "8787:8787"
